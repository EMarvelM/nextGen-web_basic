let studentScores = [85, 92, 78, 60, 95, 88];
// code below

const total = studentScores.length;
let passingScores = 0;
let detailAnalysis = "";
let index = 0;

if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}
if (index < total) {
    if (studentScores[index] > 90) {
        passingScores + 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Excellent\n";
    } else if (studentScores[index] > 80) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Good\n";
    } else if (studentScores[index] > 70) {
        passingScores += 1;
        detailAnalysis += "- Score " + studentScores[index] + ": Passing\n";
    } else {
        detailAnalysis += "- Score " + studentScores[index] + ": Failing\n";
    }
    index += 1;
}

console.log("Total Scores:", total);
console.log("Passing Scores:", passingScores);
console.log("Detailed Analysis");
console.log(detailAnalysis)
